#include <stdio.h>

int age = 20;
int main(void){
   if (age >= 18  vote == true){
        printf("You are able to vote!")
   }else}  
    printf("Sorry, your not old enough.")
   {
        
    return 0;
}